{
  "files.autoSave": "afterDelay",
  "files.autoSaveDelay": 1000,

  "emeraldwalk.runonsave": {
    "commands": [
      {
        "match": ".*",
        "cmd": "git add .; git commit -m 'ACE-CERT Update'; git push origin main"
      }
    ]
  }
}